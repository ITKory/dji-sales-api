# Local demo defaults; copying this file to .env is optional.
POSTGRES_DB=sales_performance
POSTGRES_USER=sales_performance
POSTGRES_PASSWORD=sales_demo_local_only
API_PORT=5026
FRONTEND_PORT=5173
# Disable when initializing a non-demo database. Dates/data are fixed in sales-demo-v1.
SEED_ENABLED=true
