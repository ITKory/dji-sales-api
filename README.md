[# Sales Performance
Refunded — это полностью возвращённый чек, и он целиком исключается из метрик, как и Cancelled.
Пример: оплачены чеки на 100 и 200, второй полностью возвращён → Revenue=100, SalesCount=1, AverageCheck=100; прибыль и себестоимость только первого чека. Если возвращены оба — все метрики равны 0, обе строки остаются в Recent Sales.

## 4. Что не успели за 8 часов
1. Структурировать код и избавиться от избыточности.
2. Вынести rawSQL в отдельные sql файлы для
3. Переписать фронт по fsd, раскидать дуближ на переиспользуемые компоненты.

## 5. Что улучшили бы дальше (production roadmap)
1. Импорт / Экспорт
2. Частичные возвраты
3. Кеширование на сервере
4. Batch iterator для обновления больших объемов данных
5. Background services для мануального или запланированного запуска пересчета данных.
6. Самописный EventBus в рамках этого проекта был бы хорошей заменой брокеров сообщений.

## Запуск одной командой

Из корня проекта, при запущенном Docker с Docker Compose:

```sh
docker compose up --build -d --wait
```

Команда собирает фронтенд и API, запускает PostgreSQL, применяет миграции,
загружает демоданные и ждёт готовности сервисов.
Локально устанавливать Node.js и .NET для этого не требуется.

- Интерфейс: http://localhost:5173
- API / Swagger: http://localhost:5026/swagger
- Проверка готовности API: http://localhost:5026/health/ready

Фронтенд отправляет запросы на `/api` через nginx к API внутри Docker-сети.
После изменения кода повторите команду запуска, чтобы пересобрать приложение.

Настройки по умолчанию работают без `.env`. При необходимости скопируйте
`.env.example` в `.env` и измените `FRONTEND_PORT`, `API_PORT` или настройки БД.
Демоданные охватывают 24.09.2025–23.09.2026 (набор `sales-demo-v1`), поэтому за
текущий период данных может не быть — выберите диапазон внутри этого интервала.

Логи:

```sh
docker compose logs -f
```

Остановка с сохранением данных PostgreSQL:

```sh
docker compose down
```
]()
